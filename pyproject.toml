[project]
name = "thread-viewer"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "tqdm",
	"opencv-python",
	"uvicorn[standard]",
    "fastapi",
    "websockets",
]

[project.optional-dependencies]
dev = [
    "ruff",
    "black",
]
